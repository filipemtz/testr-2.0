services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: 
      - backend/.env
    expose:
      - 8000
    ports:
      - "8000:8000"
    
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    env_file:
      - frontend/.env
    expose:
      - 8080
    ports:
      - "8080:8080"
    depends_on:
      - backend

  judge:
    build:
      context: ./backend
      dockerfile: Dockerfile.judge
    env_file: 
      - backend/.env
    depends_on:
      - backend


  
